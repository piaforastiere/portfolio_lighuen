import React from 'react' // that's it
import ReactDOM from 'react-dom'

document.addEventListener('DOMContentLoaded', function() {
  ReactDOM.render(
  <SingularSwiper />, document.getElementById('singular_swiper')
  )

}, false);
